meta:
  TEMPLATE: true

{{ .ServiceName }}_server:
  GRPC_ADDR: "localhost:CHANGE_ME"
  GRACE_PERIOD: "3s"

http:
  METRICS_LISTEN_ADDR: ":CHANGE_ME"

logging:
  LEVEL: "debug"
  FORMAT: "json"
  DEBUG_NAME: ""

flagd:
  flags_path: "./config/flags.flagd.json"